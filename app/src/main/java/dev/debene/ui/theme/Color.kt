package dev.debene.ui.theme

import androidx.compose.ui.graphics.Color

// Gandula palette — retro-feel, modern dark UI with an electric-blue accent.
// (Mirrors gandula.debene.dev: near-black canvas, vivid blue brand, Inter/mono.)

val ElectricBlue = Color(0xFF4F7DFF)
val ElectricBlueDim = Color(0xFF3A5FC8)

val Ink = Color(0xFF0A0B0F) // app background — near black
val Surface1 = Color(0xFF14161D) // cards
val Surface2 = Color(0xFF1C1F28) // raised / variant
val OutlineDim = Color(0xFF2A2E3A)

val TextHigh = Color(0xFFE8EAF0)
val TextMid = Color(0xFF9AA1B2)

val BlueContainer = Color(0xFF18243F) // header / primary container
val OnBlueContainer = Color(0xFFBCD0FF)

val PositiveGreen = Color(0xFF35C66B)
val DangerRed = Color(0xFFFF5C6C)
val DangerContainer = Color(0xFF3A1A20)
val GoldTertiary = Color(0xFFE8C24B) // cup accent
val GoldContainer = Color(0xFF241F12)
